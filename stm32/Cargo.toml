[package]
name = "stm32"
version = "0.1.0"
edition = "2024"

[dependencies]
engine = { path = "../engine" }

cortex-m = "0.7"
cortex-m-rt = "0.7"

panic-halt = "1"

embedded-hal = "1"

stm32f4xx-hal = { version = "0.23", features = ["stm32f446"] }